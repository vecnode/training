# runs

Training checkpoints and the trained LoRA adapter (`llava15_lora/final_adapter/`),
written by `train_llava15_lora.py`. Git-ignored — these are real trained results,
not cheaply regenerable, so back them up separately if they matter (this
README is only here to keep the folder visible in git).
