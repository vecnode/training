# data

Prepared JSONL dataset (`llava15_train.jsonl`) built by `build_llava15_dataset.py`
from the pre-training repo's OCR + SUMMARIES CSVs. Git-ignored — rebuild it
locally rather than expecting it to be checked out.
