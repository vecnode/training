# runs

Training checkpoints, `final_adapter/`, and logs written by
`train_qwen3b_lora.py`. Git-ignored — these are real trained artifacts, not
rebuildable from source, so back them up separately if they matter.
