# data

Prepared JSONL dataset (`llava15_train.jsonl`) built by `build_llava15_dataset.py`
from a local CNN/DailyMail Parquet dump. Git-ignored — rebuild it locally
rather than expecting it to be checked out.
